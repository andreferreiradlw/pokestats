version: 2.1
orbs:
  heroku: circleci/heroku@0.0.10
workflows:
  version: 2
  build:
    jobs:
      - heroku/deploy-via-git:
        filters:
          branches:
            only:
              - master
