//
// styles
import { SectionTitle } from '@/BaseStyles';
// components
import Box from '@/components/Box';

const MoveMachines = (): JSX.Element => {
  return (
    <Box flexalign="flex-start" flexjustify="flex-start" flexgap="1em">
      <SectionTitle>Machines</SectionTitle>
    </Box>
  );
};

export default MoveMachines;
